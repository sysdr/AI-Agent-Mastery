import React from 'react';
import { Container, Typography } from '@mui/material';

const SecurityMonitor = () => (
  <Container><Typography variant="h4">Security Monitor</Typography></Container>
);

export default SecurityMonitor;
